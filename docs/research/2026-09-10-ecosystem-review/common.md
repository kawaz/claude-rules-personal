# エコシステムレビュー指摘 — 全プロジェクト共通

外部レビュー (2026-09-09〜10) のうち、特定リポに閉じず横断で効くもの。温度感は [README](README.md) を参照。

## バックポート候補パターン (P-*)

各プロジェクトで良かった形を、プロジェクト横断の knowledge にする候補。「問題の指摘」ではなく「拾って標準にすると他リポにも効くもの」。時期はどれも急がない (ccmsg 安定後にまとめて、または該当 reference を触るついでに)。「既存」列は rules-personal の今日時点の reference / rule に同じ内容が既にあるかを見た結果。

| # | 出所 | パターン | 反映先 | 既存 |
|---|---|---|---|---|
| P-1 | die `findings/2026-06-28-tty-detection-cross-os.md` | クロス OS の TTY 判定: `isatty` だけで足りない場面、Windows は `GetConsoleMode` → Cygwin/MSYS pty は名前判定の三段構え、Windows の NUL は char device、bare-TTY と pipe と file redirect の 3 区別 | `reference/tty-detection.md` を新設 (単独本文)。発火語: isatty, TTY 判定, Cygwin pty, GetConsoleMode, NUL, stdin が端末か | 無し。hyoui / cache-warden / stable-which の TTY 判定にそのまま効く。shimux → cmux-msg → die → hyoui と拾い集めてきた成果の置き場になる |
| P-2 | die v0.0.3 → v0.2.0 のコミット | リリースゲートの dogfood: わざと落ちるリリースを 1 回打ち、ゲートが効くことをリリース履歴で証明する (「expected to fail」を message に書く) | `reference/justfile/release-pipeline.md` に「ゲートを足したら 1 回わざと落として確かめる」節 | 無し |
| P-3 | die DR-0006 | default の意味を最小に定義し、完全性は opt-in flag に寄せる (`-n` = cat 同等の byte 透過、default = 「カーソルが崩れない」だけ)。default に完全性を持たせると仕様が「cat との差分」で語られて発散する | `reference/cli-design-preferences.md` に「default の意味の決め方」節 | 無し (bool フラグ / 引数位置の好みはあるが、default の意味論の決め方は無い) |
| P-4 | die DR-0003 / DR-0007 (4 言語比較)、cache-warden draft-DR-0031 (TouchID: ライブラリ側を選んだ例) | 選定 (言語 / 自作 vs 既存ライブラリ) は、まず自作してテストで品質を上げ切ってから、同じ仕様で候補を複数書かせて良い点・悪い点を比較する。圧倒的にライブラリが良ければ使い、良い点があれば取り込んで自作を強化する。採用しなかった実装は archive bookmark に残す。比較の前に諦めない | `reference/design-spec/spec-preflight.md` か `design-thinking` に「選定の手順」として 1 段落 (memory では足りない: 発火頻度が高い) | 無し |
| P-5 | hyoui `docs/decisions/INDEX.md` | DR INDEX に Status 列 (✅ 実装済 / 🟡 部分 / ⬜ 未実装 / 🔁 Superseded / N/A) を持ち、DR ↔ 実装の双方向整合を INDEX 自体で見えるようにする (H-5 の「経緯を抜く」と対で) | `reference/docs-authoring/templates/decisions/INDEX.template.md` に Status 列を追加。`design-impl-bidirectional-check` rule から「INDEX の Status 列が B 方向の入口」と 1 行参照 | 無し (テンプレは Active / Archived / Superseded の節分けだけ) |
| P-6 | hyoui CLAUDE.md「検証主義」 | サンプル 1 種で結論を出さない。対象の性質が違う最低 3 category (hyoui なら alt screen / line-oriented / REPL) で検証し、app × mode × signal のマトリクスで「期待 vs 実態」を埋めてから実装する | `reference/testing/test-coverage-checklist.md` に「category 横断 (サンプル 1 種で断定しない)」観点を追加。3 category の具体は hyoui 固有なので例として | 無し (境界・同値・デシジョンテーブルはあるが「対象の category を跨ぐ」観点が無い) |
| P-7 | hyoui CLAUDE.md「道具揃った段階の運用」 | 観測道具自体に bug があったら道具を最優先で直す (道具が信用できないと以降の判断が全部崩れる) | `for-all/rules/empirical-verification.md` の「観測道具を使う」節に 1 文 | 無し |
| P-8 | hyoui CLAUDE.md「Anti-patterns」 | rule の根拠を「実際にやらかした」記録で持つ。禁則の文面より、踏んだ事実 (SIGCHLD の再発明、サンプル 1 で断定、マトリクスを先送り) の方が説得力がある | `for-all/rules/rule-writing-guidelines.md` の記述原則に「禁則には踏んだ実例を 1 行添える (無いなら rule にする根拠が弱い)」を追加 | `sloppy-ai-patterns` は実質これだが、原則としては書かれていない |
| P-9 | llm-gateway DR-0014「core は provider の名前を 1 つも知らない」 | アーキテクチャの不変条件を **grep で機械検査できる文** で書く。「疎結合」ではなく「core crate に provider 名が出ない」 | `reference/design-spec/spec-preflight.md` の「やらないことに反する変更を検出するテストを書く」節に「不変条件は grep / テストで判定できる文で書く」を追加。ccmsg §11.3 (M1〜M6 をテストで固定) と同型 | 近い (検出テストの話はある)。「文の書き方」が足りない |
| P-10 | llm-gateway DR-0014 §2 | capability の非対称は optional 取得 (`fn quota_api(&self) -> Option<&dyn QuotaApi>`) で表し、「無い」を空実装やエラーで表さず型として無いことを示す | `reference/design-spec/` か新設 `reference/design-patterns/optional-capability.md`。Rust 固有に見えるが TS の optional method / Go の interface assertion でも同型 | 無し |
| P-11 | llm-gateway DR-0024 → findings → DR-0027 | 積み上げた仕掛けを実測 1 本で前提ごと切り捨てる時、新 DR の文脈節に「旧 DR の複雑さの何が前提から派生していたか」を列挙する (捨てるものの正当化を、機能でなく前提の崩れで書く) | `reference/design-spec/spec-finishing.md` か `docs-authoring/knowledge-timing.md` の「supersede する DR の書き方」として | 無し |
| P-12 | llm-gateway / die / ccmsg の README | README に「何をしないか」節を置き、前身や旧版の実障害を根拠にする (llm-gateway: CLIProxyAPI の偽装、die: exit code 制御は別経路) | `reference/docs-authoring/templates/README-ja.template.md` に「何をしないか」節を追加 | 無し (DESIGN 側の「やらないこと」はあるが README には無い) |
| P-13 | rules-personal `hooks/vcs-guide.sh` | hook の設計規律: ブロックしない (exit 0) / 状態確認 (status・log・diff) は除外 / 1 セッション 1 リポ 1 種別 1 回 / `cd` 先を解決して越境実行も判定 / 除外リストはリポでなくローカル設定 / `set -e` を使わず hook 自体の不具合で作業を止めない / テストは擬似 hook 入力 JSON を流して additionalContext を assert | `reference/agent-runtime/hook-design.md` を新設 | 無し。ccmsg の plugins/claude の hook にも効く |
| P-14 | ccmsg DESIGN §1.1 + §11.3 (実装済み: `no-role-branch` / `periodic` / `instance` / `single-push` / `topics` の各 test) | 「増やさないもの」を目的と同格に置き (定義 + 反する変更の例)、それぞれをテストで固定する (M1: 属性表の全 op が dispatch を通る、M3: 周期タイマーの一覧と根拠コメントの存在、M4: 起動 → 停止 → 起動でファイルが増えない、M5: push 抑制の実装が 1 つ、M6: 自 config home 以外を読まない) | `reference/testing/` に「不変条件テストのカタログ」として (M1〜M6 の型を一般化)。`design-thinking` の「何を増やしたくないかを目的と同格に置く」は既にあるので、テストで固定する側だけ | 半分ある (design 側はある、testing 側が無い)。v2 で 6 本全部実装されたので、型を抽出する材料は揃った |
| P-15 | ccmsg DESIGN §1.3 | 周期タイマーと「周期ではない時間の値」(窓・期限・打ち切り) を区別し、後者も定数の doc comment に根拠を書く。根拠を書けない `setInterval` は禁則 | `reference/agent-runtime/event-driven-alternatives.md` に「時間の値の 4 分類 (周期 / 窓 / 期限 / 打ち切り) と根拠の書き方」 | 無し (sleep / polling の代替はあるが、正当な時間の値の扱いが無い) |
| P-17 | stable-which の利用側 3 リポ (llm-gateway `executable.rs` / hyoui DR-0031 / cache-warden DR-0019 §2.5) | `service register` で焼き込むバイナリパスは stable-which で選び、`durable` でなければ警告または拒否する。`register` を冪等にする (llm-gateway DR-0028 決定 7 の「安定パス解決と冪等性」) | `reference/cli-daemon-subcommands.md` の `service register` 行に「焼き込むパスの選び方」を 1 段落 | 無し (reference は stable-which に触れていない。3 リポとも同じ判断をしているのに標準側に無い) |
| P-18 | stable-which DR-016 | 三値判定 (Durable / NotDurable / Unknown) で、肯定条件を満たしたものだけ良い側に倒し、Unknown は安全側 (not-durable) として扱う。否定条件の不在で良い側に倒す (DR-003 の旧方式) と、知らないパターンが全部「良い」になる | `reference/design-spec/spec-preflight.md` か `design-thinking` に「判定は allow-list で、未知は安全側」を 1 行。passkey の attestation / peer の endpoint 束縛など認証系の判定と同型 | 無し |
| P-19 | stable-which の findings + `durability.rs` の定数表 (kawaz の dotfiles 整理の経験) | 各パッケージマネージャ (Homebrew / nix home-manager / nix-darwin / mise / aqua / rye / cargo / go / npm / claude / cursor-agent) が「どこに何を置き、upgrade で何が動くか」の表。参照パスと realpath の 2 段構造、バージョン識別子の段数がマネージャごとに違うこと、`~/.nix-profile/bin` が home-manager 環境では空 | `reference/path-managers.md` を新設 (`app-file-placement.md` の隣)。発火語: brew upgrade で壊れた, PATH, shim, realpath, Cellar, nix profile, mise installs | 無し (allow-list として stable-which に焼き込まれた形でしか残っていない) |
| P-20 | bump-semver DR-0020 `vcs is` | 述語コマンドは exit 0 = 真 / 1 = 偽 / 2+ = エラーで、未知の述語 (typo) は silent に偽を返さず必ず exit 2 + stderr (誤分岐防止) | `reference/cli-design-preferences.md` に「述語系サブコマンドの exit code」節 | 無し |
| P-21 | bump-semver DR-0020 設計哲学 2 | 定型処理用の共通レイヤは、素の下位ツールより制約を厳しくしてよい (`vcs commit` は path 必須 = 巻き込み事故防止)。薄いラッパーではなく「事故らないよう安全側に倒した opinionated な層」と位置付ける | `reference/cli-design-preferences.md` か `design-thinking` に 1 段落。llm-gateway の「介入は最小限」とは逆向きで、どちらも意図的なので両方書く | 無し |
| P-22 | bump-semver DR-0024 / DR-0028 | タスクランナーの多段引数渡しで shell glob が展開されたりされなかったりするブレを、`glob:<pattern>` prefix でツール側が受け取って吸収する。glob-backref は言語非依存 spec として `docs/specs/` に置く | `reference/justfile/recipes.md` に「recipe から引数を渡す時は shell 展開に任せず `glob:` で渡す」 | 無し |
| P-23 | bump-semver DR-0034 | 外部コマンドに渡すユーザ由来の値 (rev / tag / remote / repo) は `-` 始まりをフラグとして解釈される (`exec.Command` でも)。入力点ごとに検証か `--` の後ろに置くかを決め、実害の再現 (`vcs diff -- --output=<path>` で任意書き込み) をテストで固定する | `reference/design-spec/spec-preflight.md` の「やらないことに反する変更を検出するテスト」の隣に「外部コマンド呼び出しの信頼境界」節。cache-warden / llm-gateway / ccmsg の CLI にも当たる | 無し |
| P-24 | bump-semver DR-0035 | 複数ファイルへの書き込みは、全ファイルの置換結果を先に計算してから all-or-nothing で書き、各ファイルは temp + rename の atomic write。途中で失敗して半分だけ書き換わった状態を作らない | `reference/design-spec/` か新設 `reference/design-patterns/` に。ccmsg の inbox jsonl、cache-warden の registry にも当たる | 無し |
| P-25 | csa `TEST.md` | skill の統合テスト: `claude --setting-sources local --plugin-dir <repo> --allowedTools "Skill Bash Read" -p "<プロンプト>"` で起動し、「プロンプト → 期待する skill 使用 / 期待するサブコマンド」の表で判定する。skill が意図したプロンプトで発火するかを機械で確かめる唯一の手段 | `reference/testing/` に「skill / hook の起動テスト」として。rules-personal の `skills/itumono-*` / `pre-clear` にも当たる (今は起動テストが無い) | 無し |
| P-26 | csa `timeline --last-since` + `--last-turn` | 同じ対象を絞る 2 つのオプションを併用した時の合成規則 (「多い方を採る」) を help に明記する。併用時の挙動を書かないと、利用者は片方が無視されると思って 2 回叩く | `reference/cli-design-preferences.md` に「重なるオプションの併用規則を help に書く」 | 無し |
| P-27 | csa timeline → ccmsg dump | 長い transcript (jsonl) を別の LLM に渡す時は、生 jsonl でも要約でもなく「ユーザ入力 / 応答 / thinking をターン毎の md に落とした 1/10 の版」を丸ごと渡す。`work-principles` の「Full transcript は絶対読まない」の対になる肯定形。生成元は今は `ccmsg dump` | `reference/delegation/context-budget.md` に「transcript を渡す形」として 1 段落 (ccmsg dump の起動例付き) | 無し (禁則側だけあり、肯定形が無い) |
| P-28 | ccmsg webui の TL (csa との対比) | 分類作業は「分類済みを含む全体から未分類を探す」のではなく、分類済みは見える形で出し、未分類だけを「N items」のような残差として可視化し、1 個ずつ開いて確認できる UI / 出力にする。残差が見えるから分類が進む | `reference/findings-recording.md` か `docs-authoring/knowledge-timing.md` に「分類・棚卸し作業は残差駆動にする」。rules の監査 (findings の仕分け表) や issue の棚卸しにも同型 | 無し |
| P-29 | claude-ccmsg findings (transcript-teams-context-shapes / empty-thinking-signature / task-notification-truncation / checkpoint-rewind / claude-code-messaging-socket / native-cross-session-messaging) | Claude Code 内部 (jsonl の形、task notification の切り詰め、checkpoint、messaging socket、ネイティブ SendMessage の挙動) の実測記録。ccmsg の実装根拠だが、hyoui / rules-personal の hook / csa 後継にも効く | `reference/claude-code-internals/` を新設し、findings の「判明した事実」だけを移す (検証の詳細は元 findings に残しリンク)。`_index.md` の発火語: jsonl, transcript, isSidechain, task-notification, checkpoint, messaging socket, SendMessage | 無し (`agent-runtime/` は sleep 代替と playwright だけ)。R-7 の「最新プロジェクトで得た形を標準に」の適用例 |
| P-30 | claude-ccmsg `docs/research/*-kawaz-*-statements.md` | 設計判断の根拠になる本人の発言をセッションログから逐語で収録し、DR はそれを正本として参照する。パラフレーズで意図が変質した時は逐語が優先 | `reference/docs-authoring/knowledge-timing.md` に「裁定の逐語記録は research/ に置き DR から参照」。ccmsg dump がこの抽出の道具になる | 無し (research/ テンプレはあるが逐語記録の用途は書かれていない) |
| P-31 | claude-ccmsg `findings/2026-09-08-design-principles-audit.md` | 自分の DR 群と設計文書を、規範 (design-spec の §) を物差しにして監査し、規範の項ごと・文書ごとに commit する。「追補を本文の後ろに積む書き方が旧規定と新規定を併存させる」「会話参照 (`kawaz rNN mMM`) は room 番号だけで辿れない」といった、規範側にフィードバックすべき所見が出る | rules-personal の `docs/runbooks/fleet-audit.md` に「設計文書の規範監査」の手順として。所見 2 件は `reference/design-spec/spec-finishing.md` に反映 (追補を積まず本文を書き換える / 会話参照でなく根拠を書く) | 無し |
| P-32 | claude-ccmsg `skills/ccmsg/SKILL.md` (kawaz の運用経験) | セッション間通信の受け取り規約を対等でなく優劣付きにする: 相手は基本サブエージェントだと思え / 社交辞令禁止 / kawaz は全セッションと裏の会話を webui で見通せているので「あの作業が終わったらしいです」のリレー報告は不要。対等にすると発言のリレー・社交辞令のコンテキスト消費・相互評価によるエコーチャンバーで会議が崩れる (v1 初期の実害) | ccmsg の skill に閉じず `for-all/rules/work-principles.md` の「サブエージェントとの入出力」節か、`reference/delegation/` に「セッション間通信の規約」として。SendMessage (ネイティブ) を使う時も同じ規約が要る | ccmsg skill にはある。rules-personal 側には無い (常時 rule に要るのは「リレー不要」「社交辞令禁止」の 2 行だけ) |
| P-33 | kawaz の経路選択方針 (ネイティブ SendMessage vs ccmsg) | 相手に SendMessage で届くなら SendMessage を使う (read 操作なしで全文が相手の context に入る。ツール通知は一定サイズで truncate され「分割して取り出せ」になるので、どうせ全文読むメッセージには二度手間 + 二重 context)。`CLAUDE_CONFIG_DIR` をまたぐ相手には ccmsg (SendMessage は越境できない)。ccmsg v2 の配送 (a) 直送 / (b) inbox fallback (DESIGN §4.1) はこの判断の実装 | `reference/delegation/context-budget.md` か新設 `reference/delegation/inter-session-messaging.md` に「経路の選び方」表 (同一 config home → SendMessage、越境 → ccmsg、人へ → ccmsg room)。P-32 と同じファイルでよい | 無し |
| P-34 | ccmsg v2 `instance.ts` の `clock` 注入 + 「接続の期限を実際の deadline 経路で固定する」コミット | 期限・窓・打ち切りの時間の値は、`Date.now()` を直接読まず instance に clock を注入し、テストでは時計を進めて「WS が閉じる」のような実際の副作用を観測する。`sleep` で待つテストを書かない | `reference/testing/flaky-accountability.md` に「時間依存のテストは clock 注入で deterministic にする。timeout を伸ばす前にこれを疑う」を 1 節。hyoui H-1 の deadline hang 系にも当たる | 無し (flaky の説明責任はあるが、直し方の型が無い) |
| P-35 | ccmsg DESIGN §11.1 + `test/fixtures.test.ts` (protocol) | 契約リポが「実 wire の JSON が schema を通る」fixture を持ち、実装リポのテストが同じ fixture と検証器を読む。実装側に期待値 JSON を写さない (写すと契約が 2 箇所になる) | `reference/testing/` に「契約と実装が別リポの時の fixture 共有」。kuu の conformance fixture と同型なので、kuu / kuu.mbt の関係もこれで説明できる | 無し |
| P-36 | canddy-app-proxy (README + 設計 §1〜§6) | kawaz の実行環境の前提: ローカルの常設アプリは全部 Caddy 経由で tailscale の FQDN + https/wss で出す。TLS 終端は前段 (daemon は内側で `ws`)。apps (`kawaz.jp`) と sandbox (`tmpspace.net`) の 2 eTLD+1。証明書は LE DNS-01 (Route53) で自動更新。到達性は tailnet_only、認証は各アプリの passkey | `reference/auth-patterns/_index.md` の冒頭に「前段の前提」として 3 行。P2 (公開 FQDN + TLS) が「この環境では canddy-app-proxy が満たす」ことを後発に伝える | 無し |
| P-37 | canddy 設計 §3 | 信頼するアプリと信頼しないコンテンツは **登録ドメイン (eTLD+1) を 2 本** に分ける。サブドメイン分離では site 境界が同じで、passkey 窃取 (RP ID の registrable suffix)、cookie tossing (`domain=` 書き込み)、SameSite 無効化の 3 つが全部抜ける。`githubusercontent.com` / `googleusercontent.com` と同じ | `reference/auth-patterns/origin-separation.md` を新設 (canddy §3 を抜粋)。passkey 文書の「RP ID の制約」節から参照 | 無し (RP ID 節は「webui を別サブドメインに置くなら」と書くが、なぜサブドメインでは足りないかの 3 点が無い) |
| P-38 | canddy Caddyfile `tailnet_only` snippet のコメント | macOS は非 root プロセスに「特定 IP + port < 1024」の bind を許さない (ワイルドカード bind のみ)。tailnet IP への bind 固定でなく、全 IF listen + `remote_ip` matcher を一次防御にする。root LaunchDaemon 化 (= `~/.aws` の秘密に root で触る) より安全側 | `memory/macos-bind-privileged-port.md` に 1 行 | 無し |
| P-39 | canddy README「IAM (ACME DNS-01 用)」 | ACME DNS-01 の IAM 権限を「対象 2 ゾーン限定、`ChangeResourceRecordSets` は TXT かつ `_acme-challenge.(*.)<host>.<zone>` の名前のみ」まで Route53 の条件キー (`RecordTypes` / `NormalizedRecordNames`) で絞る。MFA 必須の role で、更新 (60 日) 時に op へのアクセスが要る | `memory/` か `reference/op-run-secret-injection.md` の隣に「ACME DNS-01 の最小権限」。AWS 固有だが考え方は汎用 | 無し |
| P-40 | canddy `certs/make-scoped-ca.sh` | テスト用の専用 CA を **nameConstraints (critical) で発行先ドメインを制限** して作り、サーバ証明書 + fullchain を出す。peer-auth の不採用表にある「検証を迂回するテスト用フラグを作らず、テスト用 CA を信頼ルートに注入する」の実装部品 | `reference/auth-patterns/peer-auth-url-identity.md` の不採用表の当該行から、スクリプトの置き場 (canddy か rules-personal の `reference/` に移す) を参照。ccmsg v2 の issue `mesh-tls-trust-root`「テスト用 CA の注入経路」がこれを使う | 無し |
| P-41 | cache-warden draft-DR-0031 (1Password の `.app` 解析) | 先行製品のバイナリをリンク解析 (`otool -L`、entitlements、プロセス構成) して、使っているフレームワークとその公開度 (PrivateFrameworks か、SDK にヘッダがあるか、最小 OS 版) を実機で確認し、Rust binding の有無まで辿ってから実装経路を決める。「1P が Rust から呼んでいる = 到達可能な実証例」を根拠にする | `reference/macos-signing/` の隣に `reference/macos-recon.md` (手順: otool / codesign -d / SDK ヘッダ確認) | 無し |
| P-42 | cache-warden DR-0003 / DR-0011 | 秘密値の TTL を soft / hard の 2 段に分け、soft 切れは再認証で延長、hard 切れは zeroize で破棄。`loaded_at` (値の取得時刻) と `activated_at` (認可の時刻) を分離し、pin で延長を固定できる | `reference/auth-patterns/` に `secret-lifecycle.md` (llm-gateway の credential store、ccmsg の token family とも同型) | 無し |
| P-43 | cache-warden `macos-process-inspect` crate + DR-0012 | UDS の接続元をプロセスツリー遡上 (pid → path / ppid / 開始時刻) で認証し、socket 層と key 層の 2 層で `allowed_processes` を持つ。crate として切り出し済み | `reference/cli-daemon-subcommands.md` に「UDS の peer 認証はプロセスツリーで」の 1 段落と crate 参照。ccmsg v2 の A4 (単一 uid) より強い境界が要る時の選択肢 | 無し |
| P-44 | cache-warden DR-0024 / DR-0028 | 秘密値の raw 読み出しを capability で gate し、プレーンテキストは `SecretBytes::with_exposed<F>` のような scope 限定アクセサからしか触れない型にする。値が関数の外へ漏れない | `reference/design-patterns/` (新設) に「秘密値の型設計」。llm-gateway の zeroize 利用と共有 | 無し |
| P-45 | kuu CONFORMANCE §0.1「green の規範」 | 準拠を主張する条件を 5 つで固定: 全 fixture ファイルが decode できる / 全 case を実行する (抜き取り不可) / skipped = 0 / mismatch = 0 / 対象 spec commit SHA を pin する (spec が bump しても pin を更新するまで新版の green を主張しない)。「概ね通った」を green と呼ばない | `reference/testing/` の P-35 (契約 fixture 共有) と同じファイルに「green の定義」節。ccmsg-protocol ↔ ccmsg にそのまま当たる | 無し |
| P-46 | kuu VISION / ROADMAP の役割分離 | VISION = 何を目指すか (寿命の長い構想、フェーズが進んでも動かない前提、未確定は「構想 (未確定)」と明示)、ROADMAP = どの順で作るか、DR = 個別判断。VISION の冒頭に「v0 からの再出発」を本人の発言引用で書き、捨てた枝 (v0 / slice) の所在を README から辿れるようにする | `reference/docs-authoring/docs-layout.md` に VISION.md の位置付けを追加 (今のテンプレは README / DESIGN / ROADMAP / STRUCTURE で VISION が無い)。「捨てた版は枝としてアーカイブし README から辿る」も同所 | 無し |
| P-47 | kuu `corpus/real-cli/` | 仕様の妥当性を、実在の CLI (brew / curl / docker / dd / cut / die …) の定義を書いて再現できるかで検証する。fixture (仕様の言い分) とは別に、現実の CLI がその仕様で表現できるかを corpus で押さえる | `reference/testing/test-coverage-checklist.md` に「実世界コーパスによる仕様検証」観点。仕様を書くリポ (ccmsg-protocol、bump-semver の glob-backref spec) に当たる | 無し |
| P-48 | claude-ccmsg `config.ts` の `session_launcher` (genTemplate) | セッション起動テンプレは command を 1 本にし、new / continue / resume / fork の差は params (`CONTINUE=-c` / `SESSION_ID` / `RESUME_AT` / `FORK_SESSION`) の有無で claude の引数を切り替える。`clean_env` (`CLAUDE*` / `ANTHROPIC_*` / `CCMSG_*` / `HYOUI_*` …) で親の環境を落とし `keep_env` で `CLAUDE_CONFIG_DIR` だけ通す (面の漏れ防止)。`direnv exec "$CWD"` で起動 (tooling-tips の形)。PROMPT に「ccmsg subscribe 起動 / pre-clear 出力があればロード」を固定で入れ、セッション開始の継続経路をテンプレ側で保証 | `reference/agent-runtime/session-launcher.md` を新設 (テンプレ 1 本 + params 差分、env の clean / keep、起動プロンプトの固定文)。ccmsg v2 の `instances[].upstream.launcher` と同じ語彙なので v2 側の DESIGN からも参照 | 無し (`claude-config-dir-isolation` rule に環境分離の禁則はあるが、起動テンプレの形は無い) |
| P-16 | llm-gateway / hyoui / ccmsg のコミットログ | `feat:` / `fix:` prefix + 日本語の意図文 (「走っている版を本人に聞き、置いてある版と並べる」) で、`git log` だけで設計の流れが追える | `reference/vcs/jj-commit-basics.md` に commit message の型として 1 節 | 無し (コマンド操作の話だけ) |

## 未評価 (読めていない)

- `privacy-*` 4 リポ、overlay 3 リポ (private)
- `claude-nandakke` / `claude-local-issue` / `claude-gh-*` 等の他 plugin 群
- セッションログ実測 (履行率の指摘は構造からの推定)
